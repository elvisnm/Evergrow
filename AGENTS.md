# Working on Evergrow

## User preferences

- Keep the game local. Do not create, connect, publish, or deploy a Site unless the user explicitly requests it.
- Use the Codex in-app browser for previews, reusing the existing local game tab.
- The user does gameplay testing and provides feedback on combat feel, movement, balance, and visuals. Do not drive gameplay, launch another browser, or run automated browser playtests unless explicitly asked.
- The user requested town/city layout captures on 2026-09-05. Static scene staging and captures in the in-app browser are authorized for that request; keep their playable session separate.
- Code-level tests, type checking, and production builds are appropriate. Existing optional browser regression tests require the user's explicit request before running.
- Keep the local development server available while the user tests. Avoid unnecessary reloads during their play session.
- Before every requested Sites publication, update and commit `CHANGELOG.md` with player-facing notes for everything since the last successful publication. Use version numbers and a date/time instead of themed release titles; keep bullets short. Group changes under New, Tweaks and Fixes; explain gameplay benefits, balance changes and save resets without implementation jargon. Keep older entries, distinguish reconstructed development recaps from verified releases, and never announce reverted/unshipped work. The main-menu reader imports this same file. Follow `docs/releases.md` and run `npm run release:check -- <last-published-source-sha>` on the clean committed source before saving/deploying the Site. Do not publish a new changed build with stale release notes.
- Commit coherent changes as checkpoints and push each checkpoint to origin, as requested by the user. Keep the existing remote; do not add or change remotes unless requested.
- This is an unreleased prototype for the user’s own testing. Prefer the cleanest current design over backward compatibility. When replacing a system, update its callers/tests and remove obsolete implementations, exports, adapters, and legacy-only tests. Do not keep old features or build compatibility layers for hypothetical consumers. Old save formats may be invalidated when a design change requires it; migrations are not generally required at this stage. The user explicitly requested both the generation-9-to-10 town upgrade (preserve characters/exploration; refresh changed town state) and the pre-editor v3-to-v4 appearance migration: preserve progress and initialize a missing look from the editor default. Mention any resulting test-progress reset. Git checkpoints provide the history.

## Current scope

Local generation 10 replaces grid towns with settlement/village/city tiers. The home is always a makeshift settlement with NO houses: tents, outdoor merchants, fire and personal storage. Families live in village/city houses. All tiers include a gambler and 96-slot per-character storage. Read `docs/settlements.md` before changing layouts, residents or these services. Earlier generation-9 statements below describe the preceding published checkpoint. This pass is local; do not push or publish without request.

Generation 10 is the local default; generation 9 was the preceding published world. `startingBiome(seed)` independently selects any of nine home climates; the origin settlement uses normal seeded naming and town/city layouts. Keep home-relative coordinates, a dry southern arrival, level-one progression and the home portal. `natural-landscape.ts` owns shared grove/rock/corridor density and dune/meadow relief; gameplay, terrain workers and reviews all use `World`. There is no separate preview generator. Generation-9 characters have an explicit upgrade to generation 10; other unsupported generations remain stored. Do not automatically delete old saves. See `docs/natural-landscapes.md`. The user approved this pass for checkpoint and Sites publication on 2026-09-07.

The local slice now includes nine smoothly blended climates: Deadwood, Verdant Forest, The Mire, Frostpine Reach, Emberfall, Amberwood, Hollow Highlands, Whispering Steppe and Sunscar Expanse. A seeded two-dimensional climate field drives terrain, 29 procedural prop families, ambient light, encounter/camp composition, and gear-profile biases. Procedural settlements have enterable buildings, and exploration maps show the visited world at multiple scales. Continue to prioritize the procedural asset engine, smooth transitions, readable navigation, and satisfying combat. The first character foundation now includes equipment, inventory, attributes, a large skill atlas, twenty unlockable active skills, one-/two-handed melee gear, bows, elemental staves, shields, dual wield, and enemy gear drops. Eight enemy archetypes now patrol, sense line of sight, commit to telegraphed attacks, and return to a home tether. Seeded camps retain member health/deaths for the run; four additional wilderness landmark types appear on the explored map. Eight local character slots now persist progression, gear, resources, world position, ground loot and camp casualties; each character has a separate explored chart. Town blacksmiths, jewelers and enchanters now provide trading, buyback, +10 enhancement, rarity upgrades, affix rerolls and geographic releveling. The Site-enabled build now includes cloud/local save selection, account-owned D1/R2 persistence and portable save files; the public Site deployment succeeded on 2026-09-06, with cross-client acceptance still pending. Android and local builds remain local-only. Read `docs/cloud-saves-sites.md` before changing persistence. Deeper item/skill content remains future work. Read `docs/character-systems.md` and `docs/weapons-and-skills.md` before extending these systems. `docs/README.md` indexes current guides, proposals and historical records; `docs/system-status.md` is the current implementation summary. Update affected guides when changing rules; keep past checkpoint counts explicitly historical. Implemented NPC/vendor rules and initial tuning live in `docs/npcs-and-vendors.md`. Save payload v4 includes explicit item recipes, commerce state, skill progression and required character appearance. Pre-editor v3 slots migrate on read with the default appearance and retain progress; unsupported v1/2 slots remain preserved. Read `docs/character-editor.md` before changing appearance, its save-before-commit editor flow or shared world/portrait projection.

The Astral skill row contains basic attack (LMB) and exactly five assignable skills (RMB, 1–4), empty on a new run. Potion (Q) and dodge (Space) occupy separate utility shortcuts beside the menu rail. Unassigned controls do nothing. Twenty major tree nodes unlock actual skills; the player assigns unlocked skills to the five slots. LMB supplies the equipped weapon’s innate melee swing, arrow, or elemental bolt without a skill unlock. Melee/bow basics are mana-free; staff basics cost four mana before reductions. There is no universal right-click cast. Fireball is an explicitly unlocked, assigned, staff-or-wand-required skill. Incompatible gear disables activation without removing assignments. Wands are one-handed bolt weapons, support shields/grimoires/orbs, and cost two base mana per bolt. Full-sized staves remain two-handed.

Gold is a per-character wallet (`wallet.ts`), credited by physical coin piles (`gold.ts`) and item sales. All NPC debits/credits must use the same validated wallet API. Keep currency rolls independent from equipment RNG, persist wallet and ground coins atomically, and route gold/XP gains into the dedicated accumulating HUD reward presentation. Gold flies to the top-left counter; XP flies to the delayed-fill rail. Level-ups use their own celebration and sound, never a duplicate feed card. Keep item pickups individually named. `reward-counter.ts`, `reward-feedback.ts`, `reward-art.ts` and `hud-experience.ts` own bounded presentation only; `/rewards.html` is an animated, save-free reward study with optional sound.

Starting mana regeneration is 1/second; manaRegen equipment/passive modifiers use mana per five seconds and are divided by five before adding. Intelligence grants +2 mana per added point; resource item growth tapers, and cost reduction tapers after 20% toward 40%. Read `docs/resource-balance.md` for source-level mana vials, recipe repricing and benchmark rules. Q is a dual potion restoring 42% maximum life and 40% maximum mana with one shared charge, usable if either resource is missing. Its two charges, 0.8-second base cooldown and one charge per eight kills live in `PLAYER_ABILITIES.potion`. Potion feedback carries actual life/mana amounts separately from healing-only skill events.

The basic attack is one repeatable action driven by character stats and equipped-weapon stats. Melee and bows use attack speed; staff/wand basics and magic skills use independent cast speed with the shared 0.8 weapon cadence factor in `equipment.ts`. `basicAttackManaCost` supplies the shared four-mana staff and two-mana wand bolt costs (before reductions) to combat and HUD; pay once at windup, never on release. First-row skills have zero cooldown but still respect action recovery and mana costs; second-row skills have higher costs and cooldowns. Keep mana-cost/cooldown reductions shared between combat and UI via resolveSkill. Casting poses and effects use the snapshotted castDuration, never a fixed animation timer. Do not reintroduce an automatic combo chain; combos may become a separate skill in future work. Keep movement and combat continuous, without hitstop. The runtime game view has no how-to text or control legend; small bindings on skill buttons are intentional. Character/inventory shortcuts (C/I) open one shared three-column window; T opens the skill atlas. These HUD shortcuts are enabled; J and the journal shortcut open Journeys. These panels pause combat, clear buffered actions, and close on Escape or their own shortcut. Search input must not trigger global game hotkeys.

Progression uses bounded regional scaling: named irregular districts derive ordinary level ranges from road travel and remoteness, with optional hazardous pockets. Home is 1–12; new encounters snapshot the player within the range, with normal −1/+1 variation, veterans +1, elites +2 and bosses +3. Camp, trial and dungeon snapshots persist; all event types are eligible from level one. Read `docs/region-scaling.md`. Read `docs/world-generation.md` before changing geography. Returning to earlier areas preserves their lower threat. Spawned enemies retain their original level, rank, damage, XP, biome, and loot seed; moving them or leveling the player does not rescale them. Source-level armor applies only to physical melee and arrows. Fire/Frost/Lightning/Arcane hits instead use matching elemental resistance (0% base, 75% cap); spirit visuals map to Arcane. Shield block applies afterward. Rings, amulets, shields and charms may roll one single-element or weaker all-element resistance affix; `resistance-content.ts` owns limits and shared channel mapping. Projectiles retain their source level and element after a caster dies. XP adjusts for the pre-award player/monster level difference. Loot uses enemy level and rank, never the player's post-kill level. Read `docs/progression-and-loot.md` before tuning these curves or tables. Normal/veteran/elite rank tables, archetype item-kind weights and biome profile weights share a loot-local RNG. Percentage item budgets taper separately from flat stats. Potions and resource pickups restore a fraction of maximum resources. These curves are an initial playtest model, with a numeric ceiling of 1,000,000 levels, not a finished infinite-number system.

Automatic enemy births wait for valid camera coverage after construction/reset. Every ambient or camp member must be fully outside that coverage and its shared visual margin; teleports, wide zoom and capacity delays do not permit visible materialization. There is no actor-count, ambient-target, camp/event/dungeon, rank or per-archetype cap. Do not reintroduce one without user direction. After the initial sixteen roaming enemies, new groups require travel and cooldown; standing still or changing zoom does not refill cleared ground. Only wholly offscreen inactive ambient actors may retire. Preserve geographic stats/reward identities and never award XP or loot for retirement or camp sleeping.

Weapon profiles explicitly own handedness. Two-handed melee weapons keep both hands attached to their hilt through movement and attacks; casting may briefly release and smoothly regrip. Bows and staves also reserve both equipment hands. Staves use the user-requested upright walking-staff carry: lead hand on the shaft beside the body, base near the feet, with the supporting hand eight art units below the lead palm throughout idle, travel and casting. The inventory visibly reserves the second hand for all 2H weapons. Bows retain their two-hand string/limb pose. One-handed melee weapons support a guarded free arm, an equipped shield, a one-handed wand, or a second one-handed melee weapon. Paired one-handed basics (including sword + wand) alternate weapons one action at a time, using each hand’s own attack snapshot. For one-handed melee weapons, do not return the free arm to a dangling idle stance.

The sword needs a clearly visible metal-gold arc that sweeps, tapers, and disperses with the blade. The user wants a dynamic arc, not its removal or a tiny glint. All weapon actions use a shared 0.8 cadence multiplier before independent attack/cast-speed bonuses. The starter sword currently attacks 1.6 times per second, and the starter bow 1.76 times per second; further cadence and impact changes should follow the user's playtest feedback. Contact effects and damage must follow the blade's sweep.

Draw HUD and text, including damage numbers, at native display resolution after world post-processing. Use locally bundled Pixelify Sans for display lettering and Barlow Medium for ALL numerals and numeric punctuation, including mixed labels. The shared Evergrow Numerals Unicode-range face comes first in Canvas and DOM font stacks; small HUD shortcut lettering still uses system sans-serif. Await loadGameFont() for both faces before rendering. Use natural glyph metrics; do not recreate letters from individually rounded pixel rectangles. Font files are a deliberate exception to procedural world/equipment art, with their license bundled locally.

Use one fixed CRT treatment with soft phosphor glow, faint scanlines, and a low-contrast RGB grille. The user requested removing settings and filter switching. Do not reintroduce selectable display modes or the HUD gear. The user-requested Escape menu now exposes compact Options for sound, bounded camera zoom, supported fullscreen and a controls disclosure; the CRT treatment stays fixed. Keep Escape pause/resume and N sound toggle; reduced motion follows the operating system automatically.

## Android / AYN Thor

Read `docs/android-thor.md` before changing native packaging or the lower display. Both app windows request 60 Hz; Android game/title drawing is capped at 60 FPS by `frame-pacer.ts`, without changing the 120 Hz simulation or browser rendering. The local Kotlin/WebView APK lives in `android/`; build/install with `npm run android:build` / `android:install`. Keep the package/signing identity and bundled asset origin stable so updates preserve saves. Never uninstall or clear device data to update the app. Routine active-play checkpoints run every 20 seconds, while explicit character/transaction checkpoints remain immediate. Sites cloud uploads batch at 30 seconds with hall/background flushes; Android sends no cloud requests. Static HUD metalwork is cached at native density, while dynamic content stays live. Skip companion map drawing/PNG encoding when its tab or item inspection hides it. Keep the upper map, short Journey list and town portal together and visible on Thor. Its firmware dashboard can cover a Presentation without changing app visibility; transport availability is not proof the companion is visible. Do not hide upper navigation based on `hasCompanion`. One primary Game owns simulation and IndexedDB; the companion only receives bounded projections and returns typed, session-checked commands. Reuse `GamepadInput`, validated character commands and durable Journey commands. Lower-screen browsing, inspection and equipping must not automatically pause or resume gameplay. Keep manual pause under player control; controller B/native Back dismisses lower-screen inspection first without also dodging. Thor's bundled WebView lacks `color-mix`; preserve opaque shared item-surface fallbacks. Native install is not authorization to deploy Sites.

## Development tools

- `/tools/` is the canonical local development homepage. Read `docs/development-tools.md` before adding reviews. Extend an existing workspace, register new views in `game/src/tools/catalog.ts`, and use shared review navigation. Keep tools outside production/Sites/Android entrypoints and `public/`.
- Tool studies own disposable in-memory state and consume runtime content/formulas/renderers. Never connect them to playable character saves. The skill playground advances a bounded isolated simulation; it does not authorize automated gameplay testing.

## Project layout

- `game/`: standalone Vite + TypeScript application; no runtime package dependencies.
- `game/src/simulation.ts` and `model.ts`: deterministic 120 Hz state, explicit tick ordering, RNG/identity allocation, interpolation snapshots and discriminated combat events. Event types require their actual payload; do not restore a catch-all optional event interface.
- `game/src/combat-damage.ts`, `combat-rewards.ts`, `combat-status.ts`, and `ground-effects.ts`: narrow headless owners for contact/death commitment, XP/loot/flask rewards, shared status application/ticking, and snapshotted delayed pulses. Simulation passes contexts rather than itself; preserve update order and exactly-once rewards.
- `game/src/enemy-state.ts`: shared transitions, alerts and interruption used by damage, statuses, streaming and AI. These modules must not import each other through the AI implementation.
- `game/src/combat-content.ts`, `encounter-director.ts`, and `combat-geometry.ts`: immutable balance definitions, encounter policy, and swept contact geometry. HUD/telegraph/action timing must consume shared definitions rather than duplicating gameplay values.
- `game/src/progression.ts`: run-local XP thresholds/overflow and level-gap reward factors. `progression-content.ts` owns shared level curves, rank multipliers, bounded percentage budgets, and source-level armor. `character.ts` awards one skill point and five attribute points per gained level, refreshing the sheet's same-level armor estimate without healing or spending points.
- `game/src/zone-progression.ts` and `encounter-scaling.ts`: seeded named regional ranges, snapshotted encounter baselines, rank offsets, monster stats and isolated loot-source identities. `encounter-director.ts` uses area level/biome for population, composition and bounded veteran/elite ranks; total kills do not increase area threat.
- `game/src/roaming-encounters.ts` and `spawn-visibility.ts`: view-aware travelling groups, bounded candidate selection, forward population retirement and shared body/effect exclusion margins. Simulation owns the travel/cooldown state; the camera only supplies coverage and never spawns enemies itself.
- `game/src/loot-content.ts` and `loot.ts`: immutable rank yield/tier tables, archetype kind weights, biome profile weights, and deterministic source-level drops. The first kill guarantees at least one item; all other quantities and rarity come from the shared tables. Ground capacity and inventory transactions remain simulation-owned.
- `game/src/character-types.ts`: shared item, equipment, attribute, modifier, skill, and character-sheet contracts. `items.ts` owns deterministic gear generation, tier/name/affix recipes, shared stat labels, and fresh starting sheets.
- `game/src/travel.ts` / `travel-command.ts`: fixed-step three-second town portal, deterministic home anchors/landings, persisted position/return ownership. `Simulation.relocate` preserves actors/loot/resources and camp memory; never reset the run for travel. Cancel at damage/input boundaries, clear encounter travel credit without resetting warmup, and establish destination camera coverage before spawning resumes. `travel-art.ts` and `/portal.html` provide procedural art/static review; permanent waypoints are not implemented.
- `game/src/npcs.ts` / `npc-art.ts`: stable procedural town service identities, reach/line-of-sight checks and role art. `commerce.ts` plans complete quotes/stock/buyback/improvements; `commerce-command.ts` persists before live commitment. `item-improvement.ts` changes explicit recipes; `items.ts:deriveItem` rebuilds stats. Shared validators enforce save bounds/ownership. `service-panel.ts` uses the registered service phase and shared item UI; equipped gear belongs above the bag.
- `game/src/inventory.ts`: validated equip/unequip/swap, transactional stowing for hand conflicts, inventory insertion, and attribute allocation. The local pack has 8×3 uniform cells, three dedicated charm rows of the same width, one item per cell and a safe overflow tray for old crowded saves. `inventory-grid.ts` owns geometry and optional saved `inventoryLayout`; 64-entry older bags remain readable and new bags allow 120 records across the equipment and charm grids. Item size *class* stays authored content data and still drives balance; it is no longer a spatial measurement. These bag records and eleven equipment slots are the character sheet's source of truth; failed actions never lose items.
- `game/src/character-save.ts`, `character-storage.ts`, and `character-session.ts`: validated versioned checkpoints, atomic eight-slot persistence with last-good backups and stale-writer checks, and the active character session. `Simulation.captureCheckpoint/restoreCheckpoint` own runtime state. Runtime `save-client.ts`, `save-worker.ts`, and `save-database.ts` move JSON/validation to a separate worker and use IndexedDB transactions with revision checks. Await session saves and save-backed commands; the game holds simulation and new commands across durable actions. Memory-only reviews use the headless repository. This storage checkpoint starts fresh local slots, without migrating localStorage.
- `game/src/title-screen.ts` / `.css`, `character-summary.ts`, and `character-portrait.ts`: forest character hall with six starter loadouts in three two-column rows (Sword + Shield / Two-handed Sword, Wand + Grimoire / Fire Staff, Shortbow / Longbow), build-power estimates and shared equipped portrait; presentation never writes saves directly. `/title.html` is a memory-only review, never a gameplay test.
- `game/src/character-commands.ts`: the runtime entrypoint for equip, unequip, bag moves, attribute/node allocation and skill assignment. Commands validate, commit and refresh projections before returning success. Game/panels must not manually sequence sheet mutations and refresh. Failures preserve state; success never heals.
- `game/src/character-stats.ts` and `character.ts`: one derivation path combining allocated attributes, equipped item modifiers, and tree bonuses into combat stats; live projection refresh, level-point rewards, and skill-slot assignment. Percent modifiers use whole percentage points.
- `game/src/weapon-content.ts`: seventeen generated weapon profiles and three shield profiles, including family, handedness, attack kind, element, base defense, and procedural silhouette. `generateItem` supports an explicit profile ID for deterministic content tools and static reviews.
- `game/src/focus-content.ts` / `focus-shapes.ts`: six grimoire/orb profiles with caster implicits, shared held/inventory/drop geometry and portrait bounds. `items.ts:itemAffixPool` owns curated wand/focus rolls for generation, improvements and service previews. Foci use normal equip, loot, commerce and save validation.
- `game/src/elemental-weapon.ts`, `weapon-emission.ts`, and `weapon-enchantment-art.ts`: mutually exclusive weapon-local melee elemental affixes, shared type colors, rig-anchored held lights and bounded procedural enchantment art. Basics/weapon skills add enchantment damage once before attack scaling; never apply it to the other hand or spell multiplier. Caster emissive cores draw after world darkening, before CRT. Two held sources share the existing 18-light cap. Generation/services/save validation must preserve the same projection; see `docs/weapons-and-skills.md`.
- `game/src/equipment.ts`: shared starter/unarmed profiles and bounded weapon attack derivation. Physical melee/arrows use attack damage; staff/wand bolts use spell damage exactly once. The character sheet owns gear; player render/combat equipment is a refreshed projection.
- `game/src/skill-tree.ts`: immutable graph of 2,182 stars, 2,923 connections, and 173 clusters (150 passive constellations, 20 skill branches and three ultimate landmarks) across Might, Cunning, and Arcana. Three terraced petals and short crosslinks support hybrid routes. Thirty-six early choices expose useful bonuses after two to five points. Each basic/advanced skill branches separately from its passive backbone, with three potency → efficiency → specialization leaves. Five balanced passive silhouettes retain their proportions under the atlas projection. Stable IDs, actual geometry bounds, validated connected allocation, bonus aggregation, and skill unlock lookup belong here. Seventeen majors in nine weapon schools retain three-point first paths and four-point advanced paths, with three additional deep Arcana ultimates; each constellation develops one authored stat specialty.
- `game/src/skill-progression.ts`: shared purchased/casting ranks, sixty selectable specialization recipes, mastery limits and optional Arcane Overload. Rank 1 opens paths; extra ranks cost one point each. Basics remain cooldown-free. Bulwark has a four-second cooldown floor; ultimates twelve seconds. Save v3 counts rank purchases in the point budget. Read `docs/skill-progression.md`.
- `game/src/skill-tree-routes.ts`: pure shortest-route previews and remaining point costs from the allocated build; previews never allocate or spend points.
- `game/src/skill-tree-art.ts` and `skill-tree-glyphs.ts`: culled native-resolution curved atlas geometry, zoom-dependent detail, and shared code-defined stat/skill engravings for Canvas nodes and DOM details. Preserve generous spacing and the distinct roles of travel stars, themed minors, notables, and skill majors. `skill-tree-light.ts` supplies gold allocated-path and blue preview-path flow over the cached atlas at 30 Hz, capped at 160 visible threads and 64 batched strokes. Preview paths take priority; reduced motion freezes light and dust.
- `game/src/skill-content.ts` and `skill-combat.ts`: shared active-skill metadata/icons, compatible-weapon selection, and recipe-based execution for twenty actions. `skill-execution-content.ts` owns typed, deeply frozen execution profiles, all skill ranges/speeds/durations/status potency, pulse counts and numeric UI labels. Add variants through recipes; new execution kinds must pass exhaustive handler checking. Combat/HUD/tree consume shared equipment requirements, costs, cooldowns, and potency. Skills require allocation and assignment; cooldowns belong to skill IDs. Melee skills use a compatible held weapon, preferring main hand; bow skills require bows, magic skills accept staff or wand and shield skills require an equipped shield.
- `game/src/ranged-aim.ts` and `enemy-body.ts`: shared body-height correction, bounded cursor-local ranged assistance, light movement prediction, and five-unit player-projectile graze tolerance. Touch/controller input additionally selects nearby visible foes within a 56-degree forward cone with modest target retention and action-specific reach; ground/self skills retain manual placement. The input adapter uses the last displayed camera and interpolated silhouettes; ground-targeted skills retain raw cursor coordinates. Released projectiles never home. Reticle feedback uses the same resolved aim; exclude dead, obscured, offscreen and out-of-range targets. Keep enemy shots and terrain collision unchanged when tuning player forgiveness.
- `game/src/projectile-launch.ts`: presentation-only weapon-tip launch for basic staff/wand bolts and active ranged skills. Keep projectile art, light and release sparks aligned; do not move collision or add range. Staff basics lift vertically, wands flick compactly, and the existing cast-speed cadence/mana payment remain authoritative.
- `game/src/projectile-combat.ts` and `projectile-art.ts`: swept projectile contacts and bounded pierce/chain/explosion payloads; separate procedural arrow/elemental presentation. Simulation advances continuous skill dashes; damage, statuses and timed effects delegate to their dedicated headless modules. Projectile and ground payloads capture their status strength and offensive critical/life-on-hit values at release. Siphon heals from actual direct-hit life removed; burns tick every 0.5 seconds without crit or life on hit.
- `game/src/skill-melee-art.ts`: bounded Cleave/Whirlwind energy sweeps consume actual attack range, arc and angular progression; Lunge afterimages follow actual movement. Keep the basic metal-gold blade trail and skill reach cues distinct.
- `game/src/inventory-panel.ts` / `.css` and `skill-tree-panel.ts` / `.css`: Astral character/inventory and Canvas skill-atlas windows built on the shared UI kit. Keep presentation separate from validated mutations. The atlas culls visible geometry and uses search/keyboard controls rather than thousands of DOM nodes.
- `game/src/item-art.ts`, `loot-art.ts`, and `loot-label-layout.ts`: shared equipment silhouettes, grounded item markers, physical resource vials and native-resolution rarity/name/level labels with bounded collision-free packing. Simulation still owns drops and proximity pickup; display offsets cannot move, award or remove items.
- `game/src/material-content.ts`, `material-response.ts`, and `material-response-art.ts`: shared immutable wood/stone/metal/ice/bone/glass/ember recipes and bounded presentation (48 bursts, 384 fragments, six brief lights). Hit/kill events retain the damaging action’s element; never infer it from current equipment on impact. Material effects own no combat, reward or save mutation. Read `docs/material-responses.md`.
- `game/src/death-presentation.ts`, `death-content.ts`, `death-rig.ts`, `death-mesh.ts`, `death-humanoid-art.ts`, `death-creature-art.ts` and `death-art.ts`: four articulated deaths per current enemy kind (36 total), with one presentation-local random selection stored per kill. Preserve independent joints and solid body-part depth; never restore whole-body shear/squash. All remains stay in actor depth order, including seated slumps. Kill events snapshot facing and impact direction; corpses never participate in AI, rewards, collision or saves. `/deaths.html` uses the same runtime art and supports every creature, variant and eight facings; `/loot.html` is the frozen loot study. Read `docs/creature-death-animations.md`.
- `game/src/enemy-ai.ts`: shared patrol, awareness, line-of-sight memory, flanking, ranged spacing, committed aim and home-return state machine. Enemy decisions call supplied mutation callbacks; damage and rewards stay in their dedicated owners.
- `game/src/goblin-camps.ts`, `warband.ts`, and `goblin-art.ts`: seeded 10–15-goblin camp variants with a veteran/elite chief, local rush/surround orders and brief leader-death morale break. Orders never mutate source stats, spawn reinforcements or bypass sanctuary/home return. Reuse camp persistence and strongbox completion.
- `game/src/camp-population.ts`: exact run-local camp ledger, activation and distance/visibility-based sleeping without population or rank caps. Preserve member health, source stats, loot seeds and deaths on streaming; sleeping never grants rewards. Only wholly offscreen disengaged groups may sleep. Keep at most 32 cached garrison actor records; detached actors compact into exact cleared/dead IDs and wounded source records. No lifetime camp quota. Read `docs/world-state-longevity.md`.
- `game/src/wilderness-sites.ts` / `wilderness-art.ts`: frozen seeded camps, watchtowers, graveyards, standing stones and caravans; shared decorative anchors, collision, ground/art/light passes, and POIs. Landmarks now support persistent choices, beacons, strongboxes and guardian trials through the separate POI layer. Read `docs/wilderness-and-encounters.md` and `docs/interactive-pois.md`.
- `game/src/hydrology.ts`, `water-simulation.ts`, `water-presentation.ts`, `water-terrain-art.ts`, `water-art.ts`, and `water-shader.ts`: sparse descending drainage, irregular lakes, rolling cell-based surface waves and bounded shader optics/reflections. World terrain, atlas and contact share water geometry; footsteps and combat events affect presentation only. Large waterways suppress duplicate biome ripple strokes. Preserve dry-bank flux boundaries, fixed-step limits, camera continuity, reduced motion and GPU cleanup. `/water.html` is a save-free authored review. Read `docs/living-water.md`.
- `game/src/world.ts`: seeded terrain, props, collision, and bounded tile, settlement and wilderness blueprint caching.
- `game/src/road-shape.ts` and `road-art.ts`: continuous road contours, blended junctions, worn cobbles, and gravel. Keep visual shoulders inside the existing clear corridor and preserve the shared road centerlines.
- `game/src/ground-surface.ts`: world-aligned terrain samples interpolated per pixel; neighboring tiles must produce the same color field without blocky sample boundaries.
- `game/src/biomes.ts`: nine immutable biome definitions and normalized weights from seeded, warped climate regions; shared ground, ambient and map colors. The 512-entry region cache cannot change generation. Read `docs/biomes.md` before extending the climate field.
- `game/src/biome-props.ts`: headless registry of 29 prop families, per-biome mixtures, collision radii, projected crowns, shadows, sway and light metadata. Generation and rendering must consume the same physical anchors.
- `game/src/settlements.ts`: deterministic town layouts, shared building/collision geometry, and points of interest.
- `game/src/world-query.ts` and `world-pois.ts`: per-request work/precision bounds and the shared POI kind registry. Cached settlements are frozen blueprints; future mutable world state belongs separately under stable IDs.
- `game/src/tree-art.ts`: eight tree families with three growth habits, rooted trunk surfaces and two moving crown layers. Crown occlusion fades foliage only; keep conservative bounds in `biome-props.ts` aligned with the art.
- `game/src/biome-wind.ts`, `biome-life-content.ts`, `biome-life.ts`, and `biome-life-art.ts`: shared traveling gusts, nine climate recipes, ephemeral grass/footstep/material reactions, decorative birds and insects, and moving canopy light. `ground-material.ts` shares actual water/road/paving contact weights with terrain drawing. Keep effects bounded, reduced motion frozen, teleports discontinuous, and gameplay state untouched. `game/forest.html` stages a collision-checked motion study and records the real renderer/CRT without gameplay ticks or saves. See `docs/living-biomes.md` and `docs/living-forest.md`.
- `game/src/ground-art.ts`, `material-art.ts`, `architecture-art.ts`, and `atmosphere-art.ts`: world-aligned deposits, bounded contextual root/litter/shadow stamps, clipped stone materials, roof/foundation weathering, localized mist and water ripples. Keep terrain crops consistent, ground decoration passable, physical anchors fixed, and reduced-motion phases frozen. See `docs/graphics-overhaul.md`.
- `game/src/environment-art.ts` and `biome-prop-art.ts`: procedural foliage, rocks, groundcover and biome detail; 24 seeded variants per family with a 96-entry sprite LRU, separate from the base prop library.
- `game/src/settlement-art.ts`: furnished buildings, roof fading, and settlement lights.
- `game/src/ground-layer.ts`: bounded terrain composition before subpixel camera sampling; keep tile joins inside one surface to avoid seams.
- `game/src/art.ts`: current entrypoint for procedural character drawing and props; it may be replaced with direct imports as callers evolve. `art-types.ts`, `art-primitives.ts`, `prop-art.ts`, `equipment-art.ts`, `character-motion.ts`, `player-art.ts`, and `enemy-art.ts` own the implementation by responsibility.
- `game/src/armor-shapes.ts`: shared helmet, cuirass and shoulder geometry for worn pieces and inventory icons; shield/weapon silhouettes stay in `weapon-shapes.ts`. Ground loot uses actual equipment shapes instead of generic class markers.
- `game/src/player-arm-rig.ts`: facing-relative arm joints with separate depth and height, projected into the 2D art; shoulder armor must use the same joint anchors and upper-arm direction.
- `game/src/attack-motion.ts`: shared angular motion for visible swings and swept melee contact.
- `game/src/character-pose.ts`: common player pose for the character, ribbon, sparks, and weapon light.
- `game/src/renderer.ts`: interpolated scene composition, camera, actors, and a separate native-resolution UI pass.
- `game/src/scene-visibility.ts`: padded viewport coverage for props/buildings/wilderness sites, invalidated when the world or visible bounds change.
- `game/src/combat-visibility.ts`: actual displayed viewport/body intersection for Arc Lightning acquisition and all chain/ricochet target selection. Never use padded spawn coverage to permit offscreen automatic targets. Missing camera coverage rejects acquisition; ordinary projectile flight stays physical.
- `game/src/camera.ts`: smooth 0.8–1.8× bounded wheel zoom and shared world/screen projection. Keep the HUD and damage-text size independent of camera zoom; terrain, object coverage, and lights must follow the visible world bounds.
- `game/src/lighting.ts`: bounded dynamic light map, cached light stamps, and prop shadows.
- `game/src/effects.ts`: bounded combat particles, trails, flashes, and damage numbers; effects never drive gameplay.
- `game/src/sword-trail.ts`: sampled world-space metal-gold ribbons following the weapon.
- `game/src/postfx.ts`: world-only WebGL bloom passes, CRT/phosphor display treatment, and clean fallback.
- `game/src/hud.ts`, `hud-frame.ts`, `hud-layout.ts`, `hud-icons.ts`, `hud-orb.ts`, `hud-experience.ts`, and `font.ts`: the selected Astral Instrument HUD, shared layout/hit bounds, calibrated silver rings and celestial engraving, skill/menu icons, animated resource glass, and native font rendering. Keep its materials and suspended-plate silhouette when extending it. The violet XP rail below the skills shows current level and XP toward the next level; smoothing and pulses are presentation only.
- `game/src/enemy-focus.ts`, `enemy-plate.ts`, and `enemy-rank-art.ts`: visual enemy hover/recent-hit focus and the native top name/health plate. Normal, veteran and elite crests use distinct iron, silver-winged and gilded-crown silhouettes; world rank badges share that heraldry. Use interpolated body bounds and the current camera transform; hover takes priority, dead/offscreen targets clear, and attack aiming remains unchanged.
- `game/src/enemy-engagement.ts`, `battle-bark-content.ts`, `battle-barks.ts`, `battle-bark-layout.ts`, `battle-bark-scene.ts` and `battle-bark-art.ts`: fixed-tick engagement edges and presentation-only Ashglass speech. Seven humanoids have 20 barks each; 30% once per encounter, 2.8-second lifetime, 0.8-second global spacing, at most three including fades. Successful rolls have a four-second admission window; temporary obstruction hides speech without restarting its lifetime. Damage numbers never suppress speech; barks draw over them. `GAME_FEATURES.battleBarks` in `game-features.ts` is the app-wide boolean (default true); false clears/suppresses speech in gameplay and reviews without adding a settings UI. Preserve independent RNG, native text, visible head attachment and collision/occlusion exclusions. No saves or gameplay effects. `/bestiary.html?barks=1` is the frozen actual-model review. See `docs/battle-barks.md`.
- `game/src/assets/fonts/` and `typography.css`: locally bundled Pixelify Sans, source/license records, and shared menu typography.
- `game/src/main.ts`: font loading, bootstrap, and hot replacement. `game.ts` coordinates systems and application phases; `game-shell.ts` owns DOM menus and controls; `panel-coordinator.ts` owns phase transitions, panel eligibility, input cleanup, save requests and return to gameplay focus; register future dialogue/vendor panels there rather than adding independent lifecycle sequences. `game-input.ts` owns held controls and action edges; `game-keyboard.ts` captures key releases and clears held input around native shortcuts/composition, which can swallow releases without a window blur; `lifetime.ts` handles reverse-order teardown and startup rollback.
- `game/src/notification-queue.ts`, `notifications.ts` / `.css`: a compact two-card feed with separate named item pickups at every rarity, first-POI discovery notifications and debounced biome entry. Keep notifications passive and concise; never infer rewards by parsing UI text. `/notifications.html` is a frozen, save-free review.
- `game/src/touch-input.ts`, `touch-hud.ts`, `touch-targeting.ts`, `touch-gesture.ts`, `touch-canvas.ts` and `touch-ui.css`: touch-only analog movement/aim, five skill targets, utilities and native-sized controls; independent contact ownership and cancellation through existing phase/save lifecycles. Mouse/keyboard/gamepad handlers and desktop HUD remain intact. Touch inventory actions use validated character commands; both maps and the atlas retain desktop input alongside touch gestures. Read `docs/touch-controls.md`.
- `game/src/ui-hit-test.ts`: shared UI boundary for combat input, enemy hover, and cursor drawing. New panels must join this boundary and clear buffered simulation inputs when changing control context.
- `game/src/ui-theme.ts`, `ui-kit.css`, `ui-icons.ts`, and `ui-components.ts`: shared DOM/Canvas palette, window/control primitives, decorative SVG icons, and abortable modal focus management. `game-menu.ts` builds start/pause/defeat windows. Read `docs/ui-kit.md` before expanding inventory or other panels; extend shared primitives rather than adding independent themes.
- `game/src/journey-state.ts`, `journey-director.ts`, `journey-command.ts`, `journey-panel.ts` and `journey-marker.ts`: bounded minimal activity guidance, explicit saved tracking, source-ledger completion and fog-safe location markers. Read `docs/journeys.md`. Source-level completion XP is committed with a durable receipt even without tracking; pinned sources never rescale. Recommended and Nearby are separate projections.
- `game/src/exploration.ts` and `world-map.ts`: discovered terrain/POIs, local exploration persistence, smoothly scrolling minimap, and interactive world map. The chart uses 768/1,536/3,072-unit terrain tiles with further overview coarsening as needed, a 384-entry LRU, and at most 256 visible tiles. Overview samples require all underlying discovery cells to be revealed. Labels/POIs declutter before hover testing. Read `docs/explored-atlas.md`.
- `game/src/exploration-save.ts` and `map-view.ts`: transactional save validation and pure map projection/zoom limits. Schemas and exports may change with the current design; no backward-compatibility or migration requirement applies to this prototype.
- `game/layouts.html` and `game/src/layout-review.ts`: dev-only static town, interior, and road scenes with PNG export using the real renderer; never advances gameplay or changes exploration saves.
- `game/rig.html` and `game/src/rig-review.ts`: dev-only frozen character poses across eight facings, without driving gameplay.
- `game/hud.html` and `game/src/hud-review.ts`: dev-only frozen healthy, damaged, and depleted player HUD/enemy plate states with PNG export; no gameplay or save access.
- `game/ui.html` and `game/src/ui-review.ts`: dev-only static review of actual windows and reusable UI primitives at desktop/narrow sizes, with frozen rendering and memory-only exploration.
- `game/encounters.html` and `game/src/encounter-review.ts`: frozen real-world camp and landmark scenes and a staged wisp warning, using Renderer/PostFX without simulation ticks or saves.
- `game/bestiary.html` and `game/src/bestiary-review.ts`: frozen eight-enemy, player loadout and procedural item art review.
- `game/biomes.html` and `game/src/biome-review.ts` / `biome-review-data.ts`: frozen real-renderer views of all nine climates and transitions, with PNG export; no simulation ticks, input, spawns or exploration access.
- `game/atlas.html` and `game/src/atlas-review.ts` / `atlas-review-data.ts`: three seeded explored-map reviews using actual World/WorldMap rendering, authored memory-only discovery and PNG export; no gameplay or saved-chart access.
- `game/progression.html` and `game/src/progression-review.ts`: dev-only interactive progression study using the actual zone, monster, XP, item and loot formulas; no simulation ticks or save access. The user can compare character/area levels, rank, archetype and biome without playing a run.
- `game/hud-directions.html`, `game/src/hud-directions-review.ts`, and `hud-concept-*.ts`: dev-only historical HUD art propositions; the user selected Astral, whose frame is shared with the runtime. Frozen scene, shared content, no gameplay/save access or theme switching. See `docs/hud-directions.md`.
- `docs/`: design documents and reference concepts, not runtime assets.

From the repository root: `npm run setup`, `npm run dev`, `npm test`, and `npm run build`. `npm run check` runs code tests, strict/core compilation, and a production build; it never runs browser gameplay tests. `npm run stats` prints source/content counts and last-build sizes. Engine tests require Node.js 22.13 or later for TypeScript stripping.

The headless core must compile with `tsconfig.core.json` without DOM or Node globals. Runtime imports must stay acyclic. Read `docs/architecture.md` for ownership and extension guidance, and `docs/system-status.md` for current content, budgets, and verified limitations.

World map opens with M, Tab while playing, or the minimap; M/Escape closes it. The map pauses combat and shows explored terrain only. N toggles audio. Interior entry is movement through an open doorway in shared world coordinates; fading roofs must never change collision. Keep towns protected from enemy spawns and pursuit. Character, gear, allocations, action bindings, resources, position, ground loot and camp casualties persist in eight browser-local slots. Each character has an independent explored chart. Use the validated checkpoint/session/repository boundary; never serialize derived stats or live combat objects. Cloud saves are available in the Site-enabled build; Android and local builds use local saves. No migration is required. Read `docs/character-saves.md`. World generation 9 adds nine blended climates and natural grove, rock and clearing distributions, retaining rivers/lakes, twelve regional wilderness families and variable 13–19-room crypts. Fresh test characters are required. The user authorized discarding old test saves; older-generation character slots remain stored but cannot load, and exploration uses a fresh namespace. No migration is required.

Equipment commits, drag eligibility and comparison previews share `planEquipmentChange` in `inventory.ts`. `equipment-preview.ts` computes complete effective stat changes, including displaced off-hand gear, without mutating the player. `item-ui.ts` / `.css` and `item-tooltip.ts` own reusable item cells, tooltip content, placement and accessibility; vendor screens must reuse them rather than copy InventoryPanel markup. External item previews take an item and character context without requiring a bag index. `/character.html?comparison=twohand` is the frozen shield-removal regression review. Short `data-tooltip` hints render through the single fixed-position `.ui-hint` surface that `ui-hint.ts` installs from `installUITheme`; never reintroduce a `[data-tooltip]::after` pseudo-element, which scrolling panel columns clip.

## Interactive POIs

`poi-content.ts`, `poi-sites.ts`, `poi-runtime.ts`, `poi-command.ts`, `poi-rewards.ts` and `poi-validation.ts` own thirteen interactive families, one active trial, compact exact claim receipts, recent claim records and persistent beacon projections and seeded partial reward delivery. Persist complete reward/choice checkpoints before live commitment. Keep all new guardians outside padded camera coverage and without any shared actor-count budget. Preserve survivor wounds, positions, waves and deaths; absence never means a kill. Blessings derive through character stats, pause in town/menus and disappear on death. `poi-panel.ts` uses the event phase and shared UI; `/events.html` is a save-free scene review with presentation-only chest replay. Generation 7 changes geography and event recipes; fresh test characters are required. Rootbound Crypt is implemented.

Enemy attack decisions are independent. Do not restore shared attack slots, attack-group turn budgets or waiting support rings. Each enemy owns its windup/recovery and checks sight/range; simultaneous melee, projectile and ground attacks are intentional. Population/rank limits govern spawning only.

## Dungeon foundation

Read `docs/dungeons.md` before extending expeditions. Rootbound Crypt uses a separate `DungeonWorld`, immutable geometry in `dungeon.ts`, persistent location/roster state in `dungeon-state.ts`, offscreen admission in `dungeon-runtime.ts`, Warden decisions in `dungeon-boss.ts`, and atomic transitions/chest claims in `dungeon-command.ts`. Floor source level is entrance geography + 1; never derive it from floor coordinates. Preserve roster wounds/deaths, finite threshold waves, chest masks, separate room discovery and the suspended surface. One live-Warden expedition, 24 living dungeon actors. Retain unfinished/reward-bearing/return-linked instances; exhausted floors retire to exact cleared-entrance IDs, never reset on reentry. No eight-instance lifetime quota. Town links include location identity. No resource refill on location changes, visible spawning, reward on unloading, or reset-by-reentry. Shared `EventChannel` handles chest opening; shared combat/loot/wallet rules remain authoritative. `/dungeon.html` is a frozen save-free review.

Runtime surface terrain uses `terrain-stream.ts`/`terrain-worker.ts` with a coarse underlay and transferred tiles; static reviews/dungeons retain the synchronous fallback. `frame-profiler.ts` is opt-in through `?profile=1`; CPU timings are not GPU timings or measured FPS. See `docs/world-performance.md`.

Current local difficulty tuning: roaming packs contain 4–6 actors (no capacity truncation), ordinary camps have eight members, and the introductory camp remains four. Enemy source damage is multiplied by 1.2 before rounding. XP thresholds through level 4 are unchanged; subsequent thresholds use the shared bounded pacing premium in `progression.ts`. Actor save validation has no count ceiling; the existing payload bound remains. No save reset is required.

## World-state consolidation and cloud-save study (2026-09-06)

`journey-controller.ts` owns runtime Journey scheduling/panels/markers; `location-controller.ts` owns travel orchestration with persistence before world/arrival changes. Game supplies live accessors and shared input/camera reset hooks. History uses exact receipts, never lossy eviction or probabilistic membership. Current character safety bound is 8,388,608 serialized string code units; chart/commerce bounds still apply. Current IndexedDB saves continue without a reset. `docs/cloud-saves-sites.md` is a feasibility proposal only; do not treat it as cloud implementation or deployment authorization.

## Cloud save boundary

`save-hub.ts` owns explicit Cloud / Local selection; only `build:site` enables cloud discovery, and Android always disables it. `cloud-client.ts` and its worker/cache keep an account-scoped durable outbox with character and explored chart together. Preserve immutable in-flight receipts while newer saves arrive. Cloud writes compare server revisions; conflicts retain the recovery branch for download and require explicit confirmation before taking the cloud version. Never silently copy or merge rosters. `game/server/worker.ts` runs only behind the Sites authenticated dispatcher; D1 owns committed revisions and private R2 objects hold bundles. Auth headers from public callers are never a supported deployment boundary. `save-bundle.ts` supplies the validated portable format for empty-slot import/export. In `npm run dev` only, `scripts/shared-saves.ts` serves one character store (`.evergrow-shared-saves.json`, or `$EVERGROW_SAVE_STORE`) so every browser on the LAN plays the same characters; `shared-save-client.ts` reads the whole store, mutates it with the same `CharacterRepository` validator the worker uses, and writes back under a version compare-and-swap. It stays Local mode -- explored charts remain in each browser's IndexedDB. `apply: 'serve'` keeps it out of every build; never give it an authentication surface or reach for it from a Site. The compact title screen uses shared local/cloud storage actions; do not add scroll-heavy flavor text or move Continue/Create below decoration.

## Equipment affixes and hybrid combat

Read `docs/equipment-affixes.md` before tuning affix pools or elemental damage. Drops and enchanting share weighted slot pools, exclusivity and specialist budgets. Movement rolls belong to boots and speed rolls to gloves, with weaker amulet exceptions. Elemental damage affixes remain rare and melee-weapon-local; Intelligence/spell bonuses scale their portion independently of Strength/physical bonuses. Sword + offhand wand is supported. Paired one-handed weapons, including sword + wand, alternate basics one action per click; holding repeats sequentially. Each hand pays its own mana cost and uses its own attack/cast speed. An unaffordable wand turn waits rather than skipping. Skills use the compatible hand. Burn/chill/lightning interrupts use shared contact rules and snapshotted potency; periodic damage cannot reapply contact effects.

## Recipe events and world expansion (2026-09-07)

`event-recipes.ts` owns immutable assault/defense/seal/timed rosters and `wave-system.ts` owns admission-aware wave clocks. Cursed chests run for ninety active-play seconds, score completed waves and pay physical loot through durable commands. No actor cap or reserved spawn slots. Offscreen, collision and sanctuary admission remain mandatory. Timed trials bank on death/departure/location changes; surviving actors retain rewards. Finite trials retain exact wounds/deaths across suspension. Seal anchors are checked and saved before starting. `chest-art.ts` and `treasure-flight.ts` share opening effects, checked landings and delayed pickup with dungeon chests. Read `docs/interactive-pois.md`, `docs/wilderness-and-encounters.md` and `docs/dungeons.md`.

## Wilderness bosses (2026-09-07)

Read `docs/wilderness-bosses.md` before changing lair placement, boss AI or hoards. Three explicit boss archetypes use a separate sparse additive layer, preserving ordinary POI identities. CampPopulation owns their 1 boss / 2 Elite / 8 Veteran rosters and exact casualties. Boss death stages a completed `bossLair` event receipt; normal automatic durable reward delivery opens its Rare-or-better hoard. No E activation, generic boss equipment/gold drop, resurrection or ambient boss spawning. Shared warning geometry lives in `wilderness-boss-content.ts`; presentation cannot change contact. `/bosses.html` is a save-free static review.

The user approved player-facing v0.2.0 for the settlement/gambler milestone. Subsequent routine publications increment the patch (0.2.1, 0.2.2, etc.). Stay below 0.5.0 until explicitly approved. Follow docs/releases.md; do not renumber technical save/world/dungeon/Sites identifiers.

Charms use the same item recipes and inventory records, with active cells 72–119 below the 12×6 bag. Charms are collected directly into their own grid and never enter the equipment bag. Only level-eligible stones grant modifiers; sorting packs both grids independently and vendors can sell charms directly. `charm-content.ts` owns six sizes, six flavors and utility weights. Gold/XP bonuses apply once at reward creation/commit, not on currency pickup or vendor payment. Read `docs/charms.md` before extending charms.

## Bulk selling from every merchant tab (2026-09-11)

Selling is offered wherever the bag is shown: bag clicks toggle a multi-item selection, the rarity chips and active-charm
consent render there, and the footer commits the `sellMany`. A dedicated `Sell all` button was tried and removed — the five
rarity chips already select everything between them, so it was a second control for the same outcome. A selection keeps its
receipt on the tab that made it; nothing reroutes the player to the Sell tab. `service-panel.ts`'s `sellable` getter holds the
two exceptions: the improve tabs, where a bag click already means "work on this item", and the chest keeper, which trades
nothing. Selection is built from `bulkSaleItems`, keeping one eligibility rule between the selector and the quote.

The chest keeper reuses the same selection for `storeMany`: `bulkTarget` says whether a bag click feeds a sale or a store, so
the rarity chips, the `sales` map and the multi-select badge are shared rather than duplicated. Storage differs in two ways
that are deliberate. Locks guard against selling, not against storing, so `bulkStorableItems` keeps them -- bulk must never be
stricter than the single store that already ignores them. And the tab's free space is checked in `quoteService`, not only in
`planService`, because the panel labels its button from the quote; without it the button would offer a store that fails on
confirm. Placement in `planService` walks a cursor forward past the slots the same batch just filled.

Taking items out is the mirror image: `retrieveMany` carries absolute stash slots, and a `takes` map holds the chest side of the
selection. The two directions are mutually exclusive -- one footer button, so selecting on either side clears the other -- which
is why the single `store`/`retrieve` arm of `storageDetail` is gone. Capacity again belongs to `quoteService`, but a pack is a
tetris grid, so `canPackItem` cannot answer for a batch: `packBatchProblem` probes the real `addInventoryItem` on a cloned sheet
so the quote and the plan cannot disagree about the charm region, overflow or footprints. Never bound a retrieval to one tab --
the panel clears the selection on a tab switch and `stash.length` is the honest range.

## Upstream sync 0.3.15 (2026-09-12)

Merged four upstream commits (Legendary reward rates, cloud deletion recovery, the 0.3.15 release notes). Two conflicts, both
in the cloud save path, both resolved by taking upstream's rewrite and re-applying one fork decision on top:

- `cloud-client.ts` -- upstream rewrote `write`/`remove` around per-slot failure tracking and deleted `commit`. Took their file
  whole, then put `randomId()` back in place of `crypto.randomUUID()`. That substitution is not a style preference: the LAN dev
  server runs over plain http, where `crypto.randomUUID` is undefined, and a save that throws there loses a character.
- `save-hub.ts` -- upstream added `statusForSlot`; the fork's `state` reports "Shared with the dev server" and routes characters
  through `SharedSaveClient`. Kept both. Upstream's plain `'Local'` status was deliberately not taken, because on this fork a
  local save may live on the container rather than in the browser, and the hall should say which.

## Dragging a trade in the service panel (2026-09-12)

Drag is a shortcut over the click-then-button flow, never a second code path for the trade itself.
A dragged cell's drop target is derived from `serviceDropZone(role, tab, source)` in `commerce.ts`, a
pure map that answers with the one zone that cell may land on: the chest stores from the bag and
retrieves from the stash, the Sell tab drops onto the offer pane, and stock or buyback drops into
the bag. Selling is tied to `tab === 'sell'` explicitly rather than inferred from the sale list --
the list also renders on the shop tab once a selection exists, and the shop tab deliberately has no
drop zone. The commit in `ServicePanel.drop` re-quotes through `quoteService` and adds the same gold
and `canPackItem` guards the footer button applies, plus the free-slot count for a store, so a drop
that would fail is refused into `.service-message` instead of attempted. Only a single item may be dragged: with two or more selected the
gesture never starts, which is the same predicate `syncMultiSelect` already uses.

One pointer path serves mouse and touch. The gesture arms after 10px of mouse movement, or after a
650ms still hold with a finger -- a finger that moves first is scrolling the pack, so the pack keeps
its scroll. That reuses the hold duration and 10px slop of `armor-tint-prompt.ts`; the panel has no
touch tooltip to compete with, because `ServicePanel.hover` returns early in `touch-mode`. Arming
takes pointer capture and a non-passive `touchmove` cancels the pan the browser would otherwise
start, and a completed drag sets `suppressClick` so the release cannot also toggle a selection.
The dragged item rides the pointer as a `.service-drag-ghost` cloned from the source cell -- its own
markup at its own measured size, so nothing resizes mid-gesture -- because HTML5 drag images are
unavailable on this path. Nothing else lights up: the container being dragged over is deliberately
left unhighlighted, so the ghost alone says what is in hand.

Where an item lands is the cell it was dropped on. `dropCell` reads that cell from the grid geometry
for both directions; an arriving item is then moved there by `place` only when the cell is free,
otherwise it keeps the first free cell `addInventoryItem` or the store already gave it. A same-
container rearrange still swaps with an occupant, matching `planInventoryMove` and the character
panel. A dragged sale asks first through the `.service-confirm` overlay, since one gesture would
otherwise turn an item into gold; every other drop commits directly, because each is reversible
from the same panel.

Dropping back into the container an item already lives in rearranges it instead of trading: the home
zone is checked before the trade zone, and the two are always different elements. The bag reuses
`moveInventoryItem` through the existing `moveItem` command; the chest gets `moveStorageItem` in
`inventory-tools.ts`, a swap between two slots of one unlocked tab, since `storageGridLayout` makes a
stored item's cell its slot index. The target cell is read from the grid geometry the same way
`inventory-panel.ts`'s `dragLocation` does, rather than from per-cell markup the service grids
do not have.

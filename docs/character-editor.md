# Character appearance editor

Integrated on `character-editor` · 2026-09-07. Local build only; no Site deployment or native install.

## Player flow

Choose an empty hall slot, enter a name/world seed and select starting gear. **Create character** opens the appearance editor before anything is saved. The editor's **Create character** button persists the chosen look with the fresh character and enters the world. Cancel returns to the hall and retains that slot's appearance draft for another attempt. A failed creation keeps the editor open with its draft and error message.

During play, open Character/Inventory (C or I) and select the artist’s palette beside the close button at the top-right of the window. Its **Edit appearance** hint identifies the action; it opens the Character tab directly, with Armor available in the same editor. The redundant Equipment heading is removed, and Level and Gear power share a silver-trimmed display beneath the portrait. Gameplay stays paused. **Save changes** waits for the durable save before applying the appearance and returning to inventory. Cancel/Escape/controller B returns without applying the draft. A failed save leaves the editor open and the live character unchanged. Editing appearance never spends gold, changes equipment, reallocates points or heals resources.

The character hall also exposes the same palette beside its trash button. It opens the selected saved character’s appearance without entering gameplay. Save updates a detached checkpoint through the repository’s revision checks, then refreshes the hall portrait and returns focus to the palette. Cancel discards the draft. Conflicting or stale saves cannot be overwritten. After a failed save, cancelling refreshes the selected character’s record and revision before editing again. Ordinary clicks on the current character do not reload it. This cosmetic edit preserves world generation, equipment, resources, location and played time; it updates the save timestamp without activating a gameplay session or upgrading the world. The disposable `/title.html` review exercises this flow against its in-memory characters.

The editor has sixteen skin palettes, sixteen hair palettes, twenty-four hairstyles, eight facial-hair choices and eight accessory choices, including None. Skin/hair colors have two pages; hair has three. Hover/focus reveals paging arrows on desktop. Phone layouts keep the character visible above an independently scrolling sheet, expose arrows continuously, use larger touch controls and retain Character/Armor tabs at the sheet's top. Keyboard and controller navigation reuse the shared focus/menu controls. LB selects Character and RB selects Armor; D-pad/left stick follows the visible palette, hairstyle and armor-part grids. A selects and B dismisses the active prompt before leaving the editor. Controller focus stays visibly outlined and scrolls clear of sticky tabs. Touch controls, including preview rotation and the close button, have at least 44-pixel target heights; paging stays visible on coarse-pointer devices. The separate face close-up is removed; the full figure is the shared preview. Character and Armor share the same panel and figure dimensions, with tab content scrolling inside the panel and stable scrollbar space. No height or body-proportion controls are included.

In inventory, directional controller navigation includes the appearance palette and Close alongside the selected equipment, bag or stats section. A activates the focused header action; LB/RB still switches sections. Open popups keep their own navigation scope.

Armor colors apply independently to helmet, chest, shoulders, gloves, legs, boots and cloak. Sixteen tints preserve geometry, shading and trim. Tints belong to the character's visual parts, so they follow replacement gear. Shoulders inherit geometry from chest equipment but have their own tint. Double-click any armor tint swatch (including the crossed white Original swatch), hold it for 650 ms with touch/pointer, or hold controller A on the focused swatch to ask whether to apply that specific color to all seven parts. Normal clicks still tint only the selected part. Confirm applies all colors to the editor draft (Original restores every part); Cancel/Escape/B dismisses only the prompt. The gesture does not change helmet visibility, and normal editor Save/Cancel still controls persistence. Scrolling, pointer cancellation, release, focus changes and interrupted controller holds cancel pending hold recognition. **Use original color** is the first palette swatch, white with a red line from top-right to bottom-left, and restores one part; **Reset all armor colors** restores every part. **Show helmet** appears only in the Armor tab and defaults off on new characters. Players can enable it during creation or later through the inventory editor. A hidden helmet remains equipped and contributes its normal stats. The live preview replaces the redundant Review look step.

## Ownership and persistence

`CharacterSheet.look` is a required `CharacterLook`: head appearance IDs, a sparse map of armor tint IDs and a boolean helmet visibility flag. `character-look.ts` validates bounded catalog IDs and rejects unknown keys. `appearance-armor-content.ts` keeps cosmetic content within the headless compiler boundary.

`appearance-command.ts`, exported through the character command entrypoint, stages a detached sheet and persists it before live commitment. Game holds new commands and simulation while saving. `character-editor.ts` owns only a disposable draft and Canvas presentation. The hall remains the owner of name, seed and starting loadout; appearance editing does not rename existing characters. The editor shows the name as plain text in both production and mockup views, without a textbox or keyboard/controller focus stop.

`playerPose` supplies the same appearance and tinted outfit to world rendering and `drawCharacterPortrait` (hall and inventory). `character-framing.ts` includes actual hair/accessory contours. Cosmetic choices do not alter collision, targeting, attack geometry or stat derivation. Long hair remains head-local procedural art without independent physics; existing hood coverage rules hide hair/ear jewelry/circlets.

Unarmed characters rest both empty hands beside the upper thighs, with soft elbow bends and wrists following the forearms. The shared rig carries this pose into the editor, portraits and world, with a small opposing arm swing while walking. Equipped off-hand items retain their holding poses. `/rig.html?equipment=unarmed` reviews the frozen idle/walking geometry across eight facings without accessing saves.

### Directional silhouette pass · local 2026-09-14

The torso now presents separate front, side and back surfaces. Chest equipment foreshortens with the facing and attack's torso turn; neck trim and cloak follow the same facing. The east/west head sectors have an actual profile skull, projecting nose, jaw and one visible eye. Hairstyles, facial hair, accessories and helmets have matching profile presentation. Diagonal heads retain the existing front/back treatment. World figures, portraits and appearance previews share these renderers; equipment hand anchors, action timing, collision and saves are unchanged. No save reset.

The existing rig review defaults to simple clothing with the helmet hidden. Equipment choices include unarmed, sword, sword/shield, two-handed sword, bow, Fire/Frost staff and wand/grimoire. Plate/cloak and helmet controls expose covered silhouettes; the Stride slider stages the walking cycle with one framing envelope across all directions. It remains a frozen presentation study without simulation or save access.

The follow-up leg pass uses `player-leg-rig.ts` for rotated hip sockets and stance tracks, fixed-length thigh/shin joints, and a compact planted/lifted foot cycle. Ground depth stays separate from lift when ordering the legs; knees bend toward the character's front while foot travel follows movement, including strafing. Boots have directional toes and flexible shafts that follow the shin, while thigh and knee armor narrow in profile. `boot-shapes.ts` takes a facing angle in radians for both worn boots and item art. These are presentation changes; movement speed, distance-driven gait timing, weapon anchors and save data stay unchanged.

Local September 15 movement review: the shared pose reads transient `locomotionVX/VY` from actual movement after collision and edge assistance. Feet therefore stride in the slide direction and scale their motion to the resolved speed, while `vx/vy` continue to own input smoothing. Blocking, stopping, travel and reset/load clear locomotion presentation; dash/dodge travel supplies the same projection. Forest and water studies explicitly stage their authored locomotion. No appearance or save-format changes.

Unarmed arms now use a 14% shorter resting limb span, with palms raised 2.5 art units and brought 1.2 units closer to the body. Soft elbow bends and the opposing walk swing remain. Unarmed actions still reach their existing targets continuously; equipped hands and the Fire Staff's resting free arm retain their prior proportions and mounts.

PR review corrections keep boot shafts proportional to the projected shin through foreshortening, with knee caps layered over the cuff. `boot-projection.ts` transforms drawing coordinates without replacing the cached source shapes, so pose changes reuse material shading and lighting changes still invalidate it. East/west facial-hair profiles preserve the moustache's clear chin, the goatee's narrow point, and the handlebar's curled tips. Regression coverage includes the south-facing gait phase `1.37445`, deformed leather/plate cuffs across eight directions, shading reuse and lighting invalidation, and distinct facial-hair silhouettes. The correction passed 36 focused tests, type checking and the production build, with static rig/catalog inspection.

The hairstyle review covers all 24 selections in eight directions. `appearance-hair-directions.ts` authors side crowns, temples, napes and rear silhouettes; side hair no longer deforms a front fringe. Braids, ponytails and buns attach behind the skull, the mohawk follows the crown, and long hair keeps the visible profile clear. Locs use a joined crown with staggered ends, curls have rounder outlines, and rear lengths use softer volume shading. The existing coverage rule still hides all hair under helmets. Catalog IDs, palettes and saves remain unchanged. Regression checks cover unobscured profile features, rear attachments, all 24 styles × 16 palettes × eight facings, mirrored profiles and helmet coverage. The pass includes static catalog inspection with Walnut, Flax and Raven palettes, 27 passing focused tests, type checking and the production build.

**Save format v4 requires the appearance recipe. Valid pre-editor v3 saves migrate automatically with the default look and keep their progress.** The default is warm skin, chestnut swept hair, no facial hair/accessory, original armor colors and helmet hidden. Players can change it later through the inventory editor. Decode adds only a missing appearance and updates the version on a parsed copy, then validates the entire checkpoint; it preserves any valid existing look and rejects malformed data. Reading does not rewrite storage. The next successful save writes v4 using existing atomic revision checks and backup handling. Character identity, timestamps, progression, gear, resources, world state and chart ownership remain unchanged during migration. Unsupported v1/2 saves remain preserved.

Local/IndexedDB saves, portable bundles and cloud validation share this decoder. Existing hosted clients remain on their deployed version until a separately requested deployment: this build accepts v3 exports, but old clients cannot read new v4 exports. Cloud cache reads apply the same migration to the returned character while leaving stored recovery data, revision tokens and immutable pending upload requests unchanged. No database-table migration is needed.

## Verification and review

The initial integration passed 767 code tests (historical), strict application/headless compilation and the local production build. New tests cover all appearance catalog IDs, malformed recipes, independent defaults/drafts, pending and failed save isolation, creation/edit/load round trips, unsupported-schema rejection and shared portrait/world pose projection. Migration tests cover full-record preservation, repeated decode, default isolation, existing-look preservation, malformed-data rejection, failed writes, stale writers, backup recovery and portable/cloud chart retention. Existing armor tests cover all part/tint combinations and source-material isolation; art tests cover facings, coverage and action poses.

`/character-editor.html` and `/character-editor-phone.html` now use the production component in memory-only harnesses. Add `?runtime` to the former to review the production save/cancel controls without writing a save. `/appearance-catalog.html` remains the procedural parts comparison. These routes are development-only. Browser review is confined to static editor/hall interactions; gameplay feel and actual device acceptance remain the user's testing.

The appearance migration checkpoint passed the full 770-test suite before the additional cloud-cache migration case, then all 20 focused appearance/cloud tests including that case. Strict application/headless compilation and the production build passed after the final changes. No user saves were manually modified during verification.

The controller/touch refinement passed 22 focused input, menu, armor and architecture tests plus strict compilation and the production build. Gesture tests exercise the production event/focus handler with simulated input, including every palette button, 650 ms holds, pointer release/scroll/cancel/focus loss, prompt cancellation and held-button confirmation prevention. Physical controller/touchscreen acceptance remains a player check. The mockups now poll standard controllers only in the focused document; tap the desired phone view before using a controller.

The preview-layout refinement passed strict compilation and the production build. In-app browser measurements confirmed identical figure, preview stage, controls, tab strip and footer bounds before/after switching Character to Armor on desktop and at 390 x 844 with production action buttons. The face canvas and its styles/drawing were removed; hairstyle thumbnails remain.


## Upstream review checkpoint · 2026-09-07

After integrating upstream main at `4d16d9b`, the complete 797-test suite, strict application/headless compilation and production build passed. Portrait regression coverage verifies that hall/inventory retain the same saved armor tint and helmet visibility as `playerPose`. Projected tint materials remain six-digit hex colors because the downstream armor shading performs additional hex blends; this preserves lit surfaces and colored shadows rather than parsing RGB strings as black.

The [25-image review gallery](captures/2026-09-07/character-editor/README.md) covers desktop creation/editing, extra palette pages, Armor, the apply-to-all prompt, the inventory entry, smartphone equivalents, all hairstyle/beard/accessory choices in four facings and six seeded random world looks. `/appearance-world.html` uses the real world renderer, lighting, CRT and native HUD in a frozen save-free stage; `?sample=0` through `?sample=5` opens individual scenes. It does not run gameplay ticks. Review helpers are development-only, including `?runtime&creation` in the editor harness. Physical touch/controller testing and gameplay acceptance remain player checks. No hosted deployment or device installation was performed.


## Upstream rebase verification · 2026-09-07

Rebased onto upstream `91b3279` (pause options and explicit saves, loot inspection/tooltips, destructible containers and combat refinements). Additive imports and headless compiler entries preserve both feature sets. Native Back dismisses an open appearance editor first; otherwise the existing Thor inspection and upstream pause-options Back handling remain intact. Keyboard/gamepad retain the editor's nested tint-prompt cancellation and upstream pause-menu navigation.

All 817 code tests, strict application/headless compilation and the production build pass at this checkpoint. The container persistence regression now runs against both pre-editor v3 (without a look) and v4 records: migration retains broken-container receipts and coins, prevents restored containers/collision/rewards from reappearing, and rejects invalid receipts. No save reset is required. The 797-test count and screenshots above describe the earlier review checkpoint; existing screenshot links remain pinned to that capture revision. No automated browser gameplay or device testing was run.
